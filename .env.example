# Copy to .env in the repo root (gitignored). Do not commit real keys.
# Used by Grok language flags (detect_scam / detector).

XAI_API_KEY=

# Optional — ElevenLabs synthetic-voice detector
# ELEVENLABS_API_KEY=
