# Custom openWakeWord ONNX models (one per scam phrase).

# Put `gift_card.onnx`, `bail.onnx`, etc. here. Filenames should match
# labels in backend/detector/keywords.py `SCAM_WAKE_PHRASES`.

# Stock alexa / hey_jarvis models are intentionally not used.
# Train: https://github.com/dscripka/openWakeWord#training-new-models
